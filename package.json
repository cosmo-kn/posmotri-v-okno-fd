{
  "name": "third-project",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"hello yo\" && exit 1",
    "check": "prettier --check './**/*.{html,css,js}'",
    "format": "prettier --write './**/*.{html,css,js}'",
    "lint": "stylelint '**/*.css'",
    "lintfix": "stylelint '**/*.css' --fix",
    "checkAll": "npm run check && npm run lint",
    "fixAll": "npm run format && npm run lintfix"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "stylelint": "^16.16.0"
  },
  "devDependencies": {
    "prettier": "^3.5.3"
  }
}
